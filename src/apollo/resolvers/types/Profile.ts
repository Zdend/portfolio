import { ProfileResolvers } from '@/types/graphql';


export const Profile: ProfileResolvers = {
    // skills: () => [],
    // projects: () => [],
    // experience: () => [],
    // phoneNumber: () => contact.phoneNumber,
    // email: () => contact.email,
    // location: () => contact.location,
}